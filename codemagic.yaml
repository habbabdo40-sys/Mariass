workflows:
  android-workflow:
    name: Mariass Android Build
    max_build_duration: 30
    environment:
      flutter: stable
    scripts:
      - name: Get Flutter packages
        script: flutter pub get
      - name: Build Universal APK
        script: flutter build apk --release --no-pub
    artifacts:
      - build/**/outputs/**/*.apk
